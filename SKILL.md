---
name: zhuli-zijin-write
description: 主力资金写入。把通达信导出的「主力资金」Excel 文件（.xls 文本 或 .xlsm/.xlsx）写入通达信自定义数据序列 ID=201（主力净额）、ID=202（主买净额）。触发词：「主力资金写入」「跑主力资金」。
---

# 主力资金写入 (zhuli-zijin-write)

把通达信「分时选股」导出的主力资金表写入自定义数据序列。

- **ID=201** ← 主力净额（日期, 数值）
- **ID=202** ← 主买净额（日期, 数值）

## 触发场景

- 董哥说「主力资金写入」「跑主力资金」→ 直接跑
- 董哥给一个 Excel 文件路径 → 用该文件跑

## 标准命令

```bash
python3 ~/.openclaw/skills/zhuli-zijin-write/scripts/zhuli_write.py "<excel文件路径>"

# 预览不写盘（先看数量对不对）
python3 ~/.openclaw/skills/zhuli-zijin-write/scripts/zhuli_write.py "<excel文件路径>" --dry-run
```

## 输入格式（自动识别）

1. 通达信导出的 `.xls` —— 实为 **Tab 分隔文本（GBK）**，首行表头
2. 真正的 Excel `.xlsm / .xlsx` —— zip 结构，自动读 sheet1

表头必须含 4 列（按列名定位，顺序无所谓）：
- `代码`（数字已丢前导 0 会自动补回 6 位，如 16 → 000016）
- `主力净额`
- `主买净额`
- `日期`（yyyymmdd）

## 写入结构（参考 ID=101 竞价最大占比）

- 每条 8 字节 = 日期(uint32 小端, 十进制 yyyymmdd) + 数值(float32 小端)
- 同日替换、按日期升序
- 文件命名 `{市场标志}_{代码}.dat`，例 `1_880654.dat`
- **已存在文件合并（其他日期不删）、不存在则创建**
- **覆盖当天语义**：本次导出里的日期会覆盖旧数据 —— 不在本次导出里的股票，其当日旧记录会被清除（导出文件 = 当天权威快照）

市场标志：`6/8` 开头→1（沪/科创/板块指数）；`0/3` 开头→0（深/创业）；`9` 开头→2（北交所）。

## 关键实现细节

- 写入目录硬编码：`/mnt/d/GP/通达信金融终端(开心果交易版)V2026/T0002/signals/signals_user_201|202\`
- `--` 无数据行（新股/停牌）自动跳过，不写 0
- 值以 float32 写入（与通达信序列格式一致，~7 位有效数字，如 630333.64 → 630333.6）

## 依赖与注意

- ID=201「主力净额」、ID=202「主买净额」已在 `datacfg.dat` 注册（type=1 序列型，60 字节/条），无需再注册
- signals 序列文件写盘无需关通达信（与隔夜单 9:27 写入同理，实测跨重启保留）
- **写完需重启通达信才显示**

## 更新记录

- 2026-09-06 v1.1：改为「覆盖当天数据」语义 —— 本次导出的日期会覆盖旧数据，不在导出里的股票当日旧记录清除（董哥 13:55 确认）；v1.0 支持 .xls(TSV)/.xlsm/.xlsx 自动识别、二进制序列写入 201/202
